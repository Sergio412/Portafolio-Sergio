{
  "name": "portafolio-sergio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "deploy": "npm run build && echo # evita que GitHub Ignore _astro > dist/.nojekyll && gh-pages -d dist"
}
,
  "dependencies": {
    "astro": "^5.8.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
